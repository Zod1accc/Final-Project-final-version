using TechnoStore.Models.DataContext;
using TechnoStore.Models;

namespace TechnoStore.Helpers
{
	public static class Searching
	{
		//public static List<Product> SearchProducts(string query)
		//{
		//	string[] keywords = query.ToLower().Split(' ');

		//	var results = _dataContext.Products
		//		.Where(p => keywords.All(k => p.Name.ToLower().Contains(k) || p.Description.ToLower().Contains(k)))
		//		.ToList();

		//	return results;
		//}
	}
}
